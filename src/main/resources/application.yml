spring:
  application:
    name: backend

  r2dbc:
    url: r2dbc:postgresql://scb-postgres:5432/scbdb
    username: scbuser
    password: 123


  datasource:
    url: jdbc:postgresql://scb-postgres:5432/scbdb
    username: scbuser
    password: 123

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

logging:
  level:
    org.springframework.r2dbc: DEBUG
    org.flywaydb: DEBUG
